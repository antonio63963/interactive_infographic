include ../appHeaderDetails/appHeaderDetails.pug

mixin popupPage(options)
  -const {title, closeClass, headerColor, buttonColor} = options || {};

  .popup-bg.hidden(class!= attributes.class)
    .popup 
      +appHeaderDetails({title, closeClass, headerColor, buttonColor})
      block
